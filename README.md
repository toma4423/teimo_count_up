# てぃもさん用カウントアップ

Streamlitを使用したシンプルなカウントアップアプリケーションです。

## セットアップと実行方法

1. **依存関係のインストール**
   ```bash
   pip install -r requirements.txt
   ```

2. **アプリケーションの起動**
   ```bash
   streamlit run app.py
   ```

## 機能
- 「カウントアップ」ボタンをクリックすると数値が1ずつ増えます。
- セッション内で数値が保持されます。
